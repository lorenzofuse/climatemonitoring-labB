<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="com.climatemonitoring.client.MainController">
    <left>
        <VBox spacing="10" style="-fx-background-color: #f0f0f0; -fx-padding: 10;">
            <Button text="Ricerca per nome" onAction="#handleSearchNome"/>
            <Button text="Ricerca per lat/lon" onAction="#handleSearchByCoordinates"/>
            <Button text="Visualizza per clima" onAction="#handleViewByClimate"/>
            <Button text="Operatore" onAction="#handleOperatorMenu"/>
        </VBox>
    </left>

    <center>
        <StackPane fx:id="contentArea">
            <VBox spacing="10" alignment="CENTER">
                <TextField fx:id="searchField" promptText="Inserisci il nome della città"/>
                <TextField fx:id="stateField" promptText="Inserisci lo stato"/>
                <Button fx:id="searchButton" text="Cerca" onAction="#handleSearchNome"/>
                <TextArea fx:id="resultArea" editable="false" wrapText="true"/>
            </VBox>
        </StackPane>
    </center>
</BorderPane>